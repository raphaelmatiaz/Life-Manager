export class Task {
    constructor(name, category, deadline, priority) {
        this.name = name,
        this.category = category,
        this.deadline = deadline,
        this.priority = priority
    }
}
